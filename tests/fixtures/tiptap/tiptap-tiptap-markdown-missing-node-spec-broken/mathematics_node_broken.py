"""Second custom node exported without a markdown serialization spec."""
from tiptap_markdown_bridge import registry
import markdown

MATH_NODE = {
    "name": "mathematics",
    "group": "inline",
}
