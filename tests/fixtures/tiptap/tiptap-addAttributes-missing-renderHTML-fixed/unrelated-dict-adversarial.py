"""Adversarial: dicts that use a similarly-shaped key set but aren't Tiptap attribute descriptors."""

REQUEST_SCHEMA = {
    "parse": "json",
    "render": "html",
}

USER_PREFS = {"default": "dark", "label": "theme"}
